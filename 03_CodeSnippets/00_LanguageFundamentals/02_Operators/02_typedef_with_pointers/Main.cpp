#include<iostream>


typedef int* intPtr;

int main() {
	
	int *j, *k, *l;			// instead of this
	intPtr m,n,r;			// we can do this

	int *a = nullptr, *b = nullptr, *c = nullptr;		
	intPtr x = nullptr, y = nullptr, z = nullptr;		
	
	std::cin.get();
}
